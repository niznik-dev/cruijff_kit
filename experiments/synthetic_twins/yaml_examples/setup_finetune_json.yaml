# Wandb Configuration
my_wandb_project: synthetic_twins
my_wandb_run_name: twin_zygosity_baseline

# Directory Configuration
input_dir_base: /scratch/gpfs/MSALGANIK/niznik/GitHub/cruijff_kit/data/yellow/synthetic_twins/
input_formatting: ''

# Dataset Configuration
dataset_label: twin_zygosity
dataset_ext: '.json'

# Model Configuration
torchtune_model_name: Llama-3.2-1B-Instruct

# System Prompt
system_prompt: 'Predict twin zygosity based on trait values'

# Training Parameters
batch_size: 1
epochs: 1
log_every_n_steps: 1
run_val_every_n_steps: 0

# Checkpoint Options
stash_adapter_weights: 'true'

# Environment Configuration
conda_env: cruijff

# Slurm Configuration
account: msalganik

# Custom Recipe
custom_recipe: cruijff_kit.tools.torchtune.custom_recipes.lora_finetune_single_device_stable
