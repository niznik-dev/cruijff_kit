*.out
*_m.yaml
*_m.slurm

*.pyc
__pycache__/
cruijff_kit.egg-info/
build/
dist/
catboost_info/

# Testing and coverage
.coverage
.pytest_cache/
htmlcov/

# Tool caches (centralized — pyproject.toml routes pytest + ruff here)
.cache/

# Environment-specific configuration (users should customize from template)
claude.local.md

# User-specific cache directory (i.e., torchtune recipes)
.claude/cache/

# Skill-creator iteration outputs (per-skill workspaces, not committed)
.claude/skills/*-workspace/

logs/
finetune.slurm
finetune.yaml
setup_finetune.yaml
inspect.slurm

# Allow generated config files inside test fixtures
!tests/fixtures/**
*G/
*.eval
